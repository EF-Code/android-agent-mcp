export const SERVER_INSTRUCTIONS = `Select exactly one authorized Android device. General control is enabled for valid non-sensitive packages; honor sensitive-package blocks and never handle secrets. Prefer semantic UI actions, verify each action with a fresh snapshot, and use the visible scrcpy mirror on the first selected-device operation. Read-only discovery precedes control; mutation tools require host approval; evidence is bounded and redacted.`;
