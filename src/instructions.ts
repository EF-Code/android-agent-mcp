export const SERVER_INSTRUCTIONS = `Select exactly one authorized Android device. For games/canvases, call visual_control_start with JPEG, then visual_control_action using normalized 0-999 coordinates; each call returns a frame. Batch a complete move in one call, prefer one swipe for drag-based boards, skip UI dumps/captures, and use settle_ms or stable_ms only for app or opponent transitions. Stop the session when done. Use semantic tools for ordinary controls. Honor sensitive-package, secret, approval, evidence, and scrcpy boundaries.`;
