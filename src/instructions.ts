export const SERVER_INSTRUCTIONS = `Select exactly one authorized Android device. General control is enabled for non-sensitive packages. For visual apps and custom canvases, use device-pixel screen_tap/screen_swipe with verify_change=false and verify_pixels=false; batch related input in screen_input_sequence, and set include_screenshot=true only when a fresh image is needed. Use ui_dump/ui_find/ui_tap for semantic controls. Honor sensitive-package blocks, never handle secrets, use scrcpy, and retain mutation approval and evidence boundaries.`;
