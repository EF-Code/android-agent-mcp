export const SERVER_INSTRUCTIONS = `Select exactly one authorized Android device. Control covers non-sensitive packages. For games/canvases, call visual_control_start, then visual_control_action with normalized 0-999 coordinates; it returns the next screenshot, so skip screen_capture between actions. Batch taps/swipes, use wait_for_change_ms only for transitions, then visual_control_stop. Use ui_dump/ui_find/ui_tap for semantic controls. Honor sensitive-package blocks, never handle secrets, use scrcpy, and retain approval/evidence boundaries.`;
