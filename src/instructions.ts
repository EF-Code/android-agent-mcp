export const SERVER_INSTRUCTIONS = `Select exactly one authorized Android device before any control action. Use semantic UI actions before coordinate fallbacks, stay inside the configured package allowlist, never handle secrets or sensitive applications, and verify every action with a fresh screen/UI snapshot. Read-only discovery is allowed before selection; mutation tools require approval. Use scrcpy only for human observation, not as the semantic control API. Capture bounded, redacted evidence only when requested.`;
